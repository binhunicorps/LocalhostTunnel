<UserControl x:Class="LocalhostTunnel.Desktop.Views.UpdatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource SoftPanelStyle}">
            <StackPanel>
                <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Update Center" />
                <TextBlock Margin="0,4,0,0"
                           Style="{StaticResource ShellSubtitleTextStyle}"
                           Text="Check for published releases and launch updater handoff." />
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource SoftPanelStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitleTextStyle}" Text="Latest Available Version" />
                    <TextBlock Margin="0,4,0,0" Style="{StaticResource CardValueTextStyle}" Text="{Binding LatestVersion}" />
                    <TextBlock Margin="0,12,0,0"
                               Style="{StaticResource ShellSubtitleTextStyle}"
                               Text="{Binding StatusMessage}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource SoftPanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Style="{StaticResource CardTitleTextStyle}" Text="Release Notes" />
                    <ScrollViewer Grid.Row="1" Margin="0,8,0,0" VerticalScrollBarVisibility="Auto">
                        <TextBlock TextWrapping="Wrap" Text="{Binding ReleaseNotes}" />
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <StackPanel Grid.Row="3" Margin="0,12,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="0,0,8,0"
                    Style="{StaticResource SecondaryActionButtonStyle}"
                    Content="Check For Updates"
                    Command="{Binding CheckForUpdatesCommand}" />
            <Button Style="{StaticResource PrimaryActionButtonStyle}"
                    Content="Install Update"
                    Command="{Binding InstallUpdateCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
